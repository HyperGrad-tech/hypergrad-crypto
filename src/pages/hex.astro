---
import ToolLayout from '../layouts/ToolLayout.astro';
import HexTool from '../components/tools/HexTool.tsx';
---
<ToolLayout slug="hex">
  <HexTool client:load />
  <div slot="seo-content">
    <h2>Hex 十六进制在线编解码</h2>
    <p>
      Hex（十六进制）是用 16 个字符（0-9、A-F）表示二进制字节的编码方式，每个字节对应 2 个 Hex 字符。
      Hex 在逆向工程、底层调试、二进制查看、密码学输出等领域广泛使用。本工具支持 UTF-8 文本与 Hex 互转，
      可自定义分隔符（空格、冒号、短横线、0x 前缀等）和大小写。
    </p>
    <h3>Hex 与 Base64 对比</h3>
    <ul>
      <li><strong>体积</strong>：Hex 膨胀 2 倍（每字节 2 字符），Base64 膨胀 4/3 倍。</li>
      <li><strong>可读性</strong>：Hex 更易人工阅读，每字符直接对应半字节。</li>
      <li><strong>调试</strong>：Hex 适合 hex dump、内存查看；Base64 适合传输。</li>
      <li><strong>常见格式</strong>：MAC 地址、颜色值、哈希值、UUID 常用 Hex；图片 Data URL、邮件附件用 Base64。</li>
    </ul>
    <h3>常见应用</h3>
    <ul>
      <li><strong>逆向工程</strong>：hexdump 查看二进制文件结构</li>
      <li><strong>密码学</strong>：哈希值、密钥、密文的 Hex 表示</li>
      <li><strong>颜色</strong>：CSS 颜色 #RRGGBB 就是 Hex</li>
      <li><strong>MAC 地址</strong>：网卡硬件地址用 Hex 表示</li>
      <li><strong>Unicode</strong>：U+XXXX 是字符 Unicode 码点 Hex</li>
    </ul>
    <h3>Hex 编码注意事项</h3>
    <p>
      <strong>Hex 不是加密</strong>：和 Base64 一样，Hex 只是编码方式，任何人都能解码，完全不提供保密性。Hex 常用于<strong>显示</strong>加密后的二进制结果——因为密文是二进制字节，直接显示是乱码，转成 Hex 才能以可读文本形式呈现。AES 加密的输出通常是 Base64 或 Hex，两者只是密文的不同表示，密文本身没变。
    </p>
    <p>
      <strong>字节序问题</strong>：Hex 表示二进制时需注意字节序（endianness）。大端序（big-endian，网络字节序）高位在前，小端序（little-endian，x86/ARM 原生）低位在前。例如整数 0x4D2 在大端序 Hex 为 <code>04 D2</code>，小端序为 <code>D2 04</code>。网络协议通常用大端序，CPU 内部通常用小端序，跨平台传输需转换（如 <code>htonl</code>/<code>ntohl</code>）。查看二进制文件时要先确定字节序，否则解读会完全错误。
    </p>
    <p>
      <strong>UTF-8 与中文</strong>：中文以 UTF-8 编码存储，每个汉字占 3 字节，即 6 个 Hex 字符。例如"中"的 UTF-8 编码为 <code>E4 B8 AD</code>，"文"为 <code>E6 96 87</code>。把 Hex 解码为文本时，必须知道原编码（UTF-8/GBK/UTF-16），否则会乱码。本工具固定使用 UTF-8解码，如果你的 Hex 是 GBK 编码的中文，需先转为 UTF-8 再用本工具。<strong>奇数长度</strong>错误是最常见的 Hex 解码失败原因——Hex 必须 2 个字符一组表示 1 字节，奇数个字符无法解析，请检查是否漏了字符。
    </p>
  </div>
</ToolLayout>
