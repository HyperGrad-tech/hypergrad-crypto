---
import ToolLayout from '../layouts/ToolLayout.astro';
import ShareTool from '../components/tools/ShareTool.tsx';
---
<ToolLayout slug="share">
  <ShareTool client:load />
  <div slot="seo-content">
    <h2>加密分享工具</h2>
    <p>
      加密分享工具让你安全地在微信、钉钉、飞书等 IM 中传递敏感信息。
      你只需设置一个只有对方知道的提示问题，输入答案，选择有效期，即可生成加密链接。
      对方打开链接后看到提示问题，输入答案就能查看内容——全程不需要电话、短信或其他额外沟通。
    </p>
    <h3>工作原理</h3>
    <ul>
      <li><strong>发送方</strong>：输入要保护的内容，设置提示问题和答案（答案作为 AES 加密口令），选择有效期，生成加密链接。</li>
      <li><strong>接收方</strong>：点击链接，看到提示问题，输入答案即可解密查看。</li>
      <li><strong>安全性</strong>：使用 AES-256-CBC 加密，PBKDF2 100,000 次迭代派生密钥。过期时间戳嵌在加密载荷内部，篡改任何比特都会导致解密失败。</li>
      <li><strong>隐私</strong>：所有运算在浏览器本地完成，数据不上传到任何服务器。</li>
    </ul>
    <h3>使用场景</h3>
    <p>
      适用于在 IM 中传递 WiFi 口令、API Key、服务器地址、临时授权码等敏感信息。
      不适合传递长期机密（因为加密强度取决于问题答案的复杂度）。
    </p>
  </div>
</ToolLayout>
